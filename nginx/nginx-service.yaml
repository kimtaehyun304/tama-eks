apiVersion: v1
kind: Service
metadata:
  name: nginx-gateway
spec:
  type: NodePort   # or LoadBalancer
  selector:
    app: nginx-gateway
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30000   # 선택 (생략 가능)